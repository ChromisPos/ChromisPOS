// promotion.bogoff
//
//   Buy one, get one free
//   This script can also be used for buy some, get some free
//   such as buy 2 get 1 free, buy 10, get 2 free etc.
//
// This is a template script for creating promotion scripts
//
// START OF USER EDITABLE VARIABLES
// For every qty_buy and customer buys, the customer can have qty_free for free
int qty_buy = 1;
int qty_free = 1;
// END OF USER EDITABLE VARIABLES

import uk.chromis.pos.forms.DataLogicSales;
import uk.chromis.pos.forms.DataLogicPromotions;

DataLogicSales saleslogic = new DataLogicSales();
DataLogicPromotions promotionlogic = new DataLogicPromotions();
saleslogic.init(session);
promotionlogic.init(session);

index = ticket.getLinesCount();
if(index == null){
    index = 0;
}
initialcount = index;
indexinit = sales.getSelectedIndex();

javax.swing.JOptionPane.showMessageDialog(null, "lines :"+ticket.getLinesCount(), "Product", JOptionPane.WARNING_MESSAGE);
